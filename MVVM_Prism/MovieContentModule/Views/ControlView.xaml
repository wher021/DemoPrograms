<UserControl x:Class="MovieContentModule.Views.ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieContentModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="130" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Button Width="50" Height="30" Content="Delete" Command="{Binding DeleteCommand}">
            </Button>
            <Button Width="50" Height="30" Content="Save" Command="{Binding SaveCommand}"></Button>
            <Button Width="50" Height="30" Content="Magic" Command="{Binding MagicCommand}"></Button>
            <Button Width="50" Height="30" Content="Magic P" Command="{Binding MagicCommand2}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
